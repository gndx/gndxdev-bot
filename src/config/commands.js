const copies = require('../data/copies.json');

const commands = {
  merch: copies.merch,
  patreon: copies.patreon,
  blog: copies.blog,
  social: copies.social,
  courses: copies.courses,
  twitch: copies.twitch,
  youtube: copies.youtube
};

module.exports = commands;
